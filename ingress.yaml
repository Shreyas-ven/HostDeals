apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hostdeals-ingress
spec:
  rules:
  - host: hostdeals.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80